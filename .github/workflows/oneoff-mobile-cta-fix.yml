name: Fix mobile homepage CTA route

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/oneoff-mobile-cta-fix.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply and validate
        run: python3 .github/oneoff_mobile_cta_fix.py
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Fix mobile homepage CTA route"
          git push origin HEAD:main
